# AGENTS.md

## Repo search guidance

- When searching this repo for implementation details, prefer source files and app config over puzzle content data.
- Exclude `public/puzzles.json` and `dist/puzzles.json` from broad text searches unless the task is explicitly about puzzle data/content.
- Exclude generated/build output like `dist/` for code investigations unless debugging deploy artifacts.
